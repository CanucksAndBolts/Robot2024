Buttons (Last Updated: 2/17/24)

xButton (active)
yButton (active)
aButton (active)
RBButton (active)
RTButton (active)
LTButton (testing)
LBButton (testing)
clawButton (testing)

Button Functions:
xButton - When pressed, toggles shooters (only) on
yButton - When pressed, turns on shooters and intake on, then off (for shooting)
aButton - When held, reverse shooters and intake out
RBButton - When held, does intake (intake disables when limit switch pressed)
RTButton - When held, reverses intake
